apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-dynamodb-service
spec:
  selector:
    app: {{ .Release.Name }}-dynamodb
  type: LoadBalancer
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
